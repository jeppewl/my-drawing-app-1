export function mapToGradient(number: number, min: number, max: number): string;
export function rgbToHex(r: number, g: number, b: number): string;
export function sin60(rad: number): number;
export function mapToHueGradient(number: number, min: number, max: number): string;
